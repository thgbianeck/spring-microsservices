spring:
  datasource:
    url: jdbc:mysql://localhost:3307/db_microsservices_exams
    username: root
    password: developer2020
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MariaDB103Dialect
    generate-ddl: true
logging:
  level:
    org:
      hibernate:
        SQL: debug